{
  "$schema": "http://json-schema.org/schema",
  "id": "SchematicForListAndEdit",
  "title": "Schema per la generazione di un elenco e form",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Nome del nuovo business object"
    },
    "project": {
      "type": "string",
      "description": "progetto in cui creare il nuovo business object"
    }
  },
  "required": [
    "name"
  ]
}
